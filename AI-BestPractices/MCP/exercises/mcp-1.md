 Create a MCP server.
1. The server exposes one service (tool) called “getProducts that returns data
    from the https://fakestoreapi.com/products api.
2. Connect A MCP Client (like Claude Desktop/github copilot) to that server and show all
   the products that cost more than 100
